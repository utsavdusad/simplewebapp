{
  "name": "06-MultipleContainers",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "node server.js",
    "build": "docker build . -t webapp",
    "tag": "docker tag webapp cbcourses/webapp",
    "run": "docker run -p 8000:3000 webapp",
    "push": "docker push cbcourses/webapp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  }
}
